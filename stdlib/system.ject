# System Standard Library Module
# Process and directory primitives are provided by the host.

export fn get_cwd()
    return cwd()
end

export fn change_dir(path)
    return _change_dir(path)
end

export fn run_process(program, arguments=[], directory=nil)
    return _run_process(program, arguments, directory)
end
