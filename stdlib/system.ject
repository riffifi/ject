# System Standard Library Module
# input, exec, env, exit, cwd, args, now, timestamp, sleep, file ops: Rust builtins (injected).

export fn get_cwd()
    return exec("pwd")
end

export fn change_dir(path)
    return exec("cd " + path)
end
