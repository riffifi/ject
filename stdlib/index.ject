# Ject stdlib overview (this file is intentionally non-executable documentation in comments only).
# For module lists see stdlib/*.ject.
# For Rust vs Ject boundaries see ../docs/NATIVE_KERNEL.md (CorLib, native-only modules, injected builtins).
