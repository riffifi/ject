print "=== Smoke: match ==="

let x = 5

# Identifier binding arm should bind `n`
match x
    n -> print "bound n=" + n
end

# Literal + wildcard arms should work
let y = 2
let label = match y
    1 -> "one"
    2 -> "two"
    _ -> "other"
end

print "label=" + label

